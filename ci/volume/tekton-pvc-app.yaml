apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-pvc-app
  namespace: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: tekton
